#include <iostream>
#include <SFML/Graphics.hpp>
using namespace std;

class player{
    public: Player(sf::Vector2f size){
        player.Size(size);
        player.setfillcolor(sf::Color::Green);
    }
    void drawTo(sf::RenderWindow &window){
        window.draw(player);
    }
    void 
    private: 
}
